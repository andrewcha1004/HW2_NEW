# OCR API Server — 환경 변수 설정
# 이 파일을 .env로 복사해서 사용하세요: cp .env.example .env

APP_NAME="OCR API Server"
APP_VERSION="1.0.0"
DEBUG=false

# OCR 언어 설정 (쉼표 구분): ko, en, ja, zh 등
OCR_LANGUAGES=["ko","en"]

# GPU 사용 여부 (CUDA 환경에서만 true)
OCR_GPU=false

# 최대 업로드 파일 크기 (MB)
MAX_UPLOAD_SIZE_MB=10
